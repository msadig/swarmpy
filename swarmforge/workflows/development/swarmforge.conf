# SwarmPy workflow config: development
# Format: window <role> <agent> <worktree>
# Agents: claude, codex, none
# Worktree: master runs in the main checkout; none creates no worktree; any other name creates .worktrees/development/<name>.
# Purpose: improve SwarmPy itself with one planner, one implementer, one test-focused verifier, and one reviewer.
window architect claude master
window coder claude coder
window tester claude tester
window reviewer codex reviewer
window logger none none
